import { StyleSheet } from "react-native";

export default StyleSheet.create({

  card: {
    padding: 15,
    borderWidth: 1,
    borderColor: "#ddd",
    borderRadius: 8,
    marginBottom: 10
  },

  node: {
    fontSize: 16,
    fontWeight: "bold"
  },

  status: {
    marginTop: 5
  }

});